import { NombreFiltroPipe } from './nombre-filtro.pipe';

describe('NombreFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
